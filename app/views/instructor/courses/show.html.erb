<br />
<div class="box-box col-10 offset-1">
  <h1 class="text-center"><%= current_course.title %></h1>
  <%= image_tag current_course.photo.to_s, class: 'img-fluid' %>
  <br />
  <br class="clr" />
  <div>
    <%= link_to 'Add a new section...', new_instructor_course_section_path(current_course), class: 'btn btn-primary float-left' %>
  </div>
  <br />
  <br />

  <ul>
    <% current_course.sections.each do |section| %>
      <li>
        <%= section.title %>
        <%= link_to 'New Lesson', new_instructor_section_lesson_path(section), class: 'float-right btn btn-primary btn-sm' %>
        <ul class="lessons">
          <% section.lessons.rank(:row_order).each do |lesson| %>
            <li data-item-id=<%= lesson.id %>>
              <b><%= lesson.title %></b>
              -
              <%= lesson.subtitle %>
            </li>
          <% end %>
        </ul>
        <br class="clr" />
      </li>
    <% end %>
  </ul>

  <script>
    $(function() {
      $('.lessons').sortable({
        update: function( event, ui ) {
          alert("LessonId: " + ui.item.data('item-id') + "Index: " + ui.item.index());
        }
      });
    });
  </script>

  <br />

  <%= link_to 'Preview as Student', course_path(current_course), class: 'btn btn-warning center' %>

</div>